specVersion: "v1beta"
serviceId: "articles-dataconnect"
location: "us-west1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "articles"
      cloudSql:
        instanceId: "news-tracker"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
